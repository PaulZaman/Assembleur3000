function ParserData(data)
	This function runs through the data section of our code and make sure the data initializations is correct.

function readData(data)
	This function creates the data corresponding to the one declared in the #data part of the code inputted by the user.

function runInstruction(instruction, stopval) 
	This function runs a single expression, callingthe corresponding alu function to execute the calculations

function run(dataANDcode, stopval) 
	This functions recieves a string element and parses and interpretes the code until a stopval is reached.